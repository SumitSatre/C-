//use the arithmetic operators

// Here we can use c syntax in cpp just to add "#include <stdio.h>".

#include <iostream>
#include <stdio.h>

using namespace std;

int main(){
    int a , b , sum, sub, div , mul;

    cout<<"Enter the first value  : ";
    cin>>a;

    cout<<"Enter the second value : ";
    cin>>b;

    cout<<"The addition is "<<a+b<<"\n";
    cout<<"The Substraction is "<<a-b<<"\n";
    printf("The substraction is %d" , a-b);


    return 0;
}

