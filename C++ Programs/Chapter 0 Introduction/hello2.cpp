/* Header Files : These are those files that store predefined functions */

/* The iostream library is an object-oriented library that provides input and output functionality using streams.*/

#include <iostream>
using namespace std;

int main(){
    
    int a= 0 , b = 0;
    
    cout<<"Enter numbers : ";
    /* This is waiting to enter its respective datatype values in it else it cannot go further*/
    cin>>a>>b;
    cout<<a*b<<endl;
    cout<<"Hello World"<<endl;
    return 999;
}